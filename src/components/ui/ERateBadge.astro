---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<span class:list={[
  'inline-block bg-tertiary text-on-tertiary text-[10px] font-bold px-3 py-1 rounded-full uppercase tracking-wider',
  className
]}>
  E-Rate Eligible
</span>
