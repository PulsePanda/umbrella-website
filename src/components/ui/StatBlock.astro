---
// Bold number + label stat display
// Implementation by Frontend Developer agent

interface Props {
  value: string;
  label: string;
}

const { value, label } = Astro.props;
---

<!-- StatBlock component placeholder -->
